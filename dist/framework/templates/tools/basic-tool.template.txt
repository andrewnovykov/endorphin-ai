/**
 * {{TOOL_DESCRIPTION}}
 * 
 * Custom tool for Endorphin AI framework
 * Generated from template
 */

import { tool } from '@langchain/core/tools';
import { z } from 'zod';
import type { EnhancedBrowserTestFramework } from 'endorphin-ai';

/**
 * Creates {{TOOL_NAME}} tool
 * @param framework - Framework instance with browser access
 * @returns LangChain tool for {{TOOL_PURPOSE}}
 */
export function create{{TOOL_NAME_PASCAL}}Tool(framework: EnhancedBrowserTestFramework) {
  return tool(
    async (params: { 
      // TODO: Define your parameters here
      // Example: selector: string;
      // Example: timeout?: number;
    }) => {
      const stepDesc = `{{TOOL_ACTION_DESCRIPTION}}`;
      
      if (!framework.currentPage) {
        throw new Error('No browser page available. Make sure a browser session is active.');
      }
      
      const page = framework.currentPage;
      
      try {
        framework.logTestStep('Starting {{TOOL_NAME}} operation', '{{TOOL_NAME_KEBAB}}');
        
        // TODO: Implement your tool logic here
        // Examples for UI automation:
        // - await page.click('button');
        // - await page.fill('input', 'value');
        // - await page.waitForSelector('.element');
        // - const text = await page.textContent('.element');
        
        // Take screenshot if needed
        await framework.takeStepScreenshot(`{{TOOL_NAME}} operation completed`);
        
        framework.logTestStep(
          'Operation completed successfully',
          '{{TOOL_NAME_KEBAB}}',
          params,
          '{{TOOL_NAME}} operation completed successfully',
          true
        );
        
        return `✅ {{TOOL_NAME}} completed successfully`;
        
      } catch (error: any) {
        framework.logTestStep(
          'Operation failed',
          '{{TOOL_NAME_KEBAB}}',
          params,
          error.message,
          false
        );
        throw error;
      }
    },
    {
      name: '{{TOOL_NAME_KEBAB}}',
      description: '{{TOOL_SHORT_DESCRIPTION}}',
      schema: z.object({
        // TODO: Define your schema here
        // Example: selector: z.string().describe('CSS selector for the element'),
        // Example: timeout: z.number().optional().describe('Timeout in milliseconds'),
      }),
    }
  );
}