/**
 * {{TOOL_DESCRIPTION}}
 * 
 * UI automation tool for Endorphin AI framework
 * Generated from template
 */

import { tool } from '@langchain/core/tools';
import { z } from 'zod';
import type { EnhancedBrowserTestFramework } from 'endorphin-ai';

/**
 * Creates {{TOOL_NAME}} UI automation tool
 * @param framework - Framework instance
 * @returns LangChain tool for UI {{TOOL_PURPOSE}}
 */
export function create{{TOOL_NAME_PASCAL}}Tool(framework: EnhancedBrowserTestFramework) {
  return tool(
    async (params: {
      selector: string;
      // Add more UI-specific parameters as needed
    }) => {
      const { selector } = params;
      const stepDesc = `{{TOOL_ACTION_DESCRIPTION}}: ${selector}`;
      console.log(`{{TOOL_EMOJI}} ${stepDesc}`);
      
      try {
        // Get the current page from framework
        const page = framework.currentPage;
        if (!page) {
          throw new Error('No page is currently open');
        }
        
        // TODO: Implement your UI automation logic here
        // Example: await page.locator(selector).click();
        
        // Take screenshot after action
        await framework.takeStepScreenshot(`{{TOOL_NAME}} completed for ${selector}`);
        
        // Log successful step
        framework.logTestStep(
          stepDesc,
          '{{TOOL_NAME_KEBAB}}',
          params,
          `Successfully performed {{TOOL_NAME}} on ${selector}`,
          true
        );
        
        return `{{TOOL_NAME}} completed successfully on ${selector}`;
      } catch (error: any) {
        // Log failed step
        framework.logTestStep(
          stepDesc,
          '{{TOOL_NAME_KEBAB}}',
          params,
          error.message,
          false
        );
        throw error;
      }
    },
    {
      name: '{{TOOL_NAME_KEBAB}}',
      description: '{{TOOL_SHORT_DESCRIPTION}}',
      schema: z.object({
        selector: z.string().describe('CSS selector or text to find the element'),
        // Add more schema definitions as needed
      }),
    }
  );
}